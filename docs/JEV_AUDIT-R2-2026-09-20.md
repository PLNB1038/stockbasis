# Jev-аудит фиксов round70 — 20.09.2026 (вечер, круг 2)

Контрольный аудит СВЕЖЕГО кода round70 (8 фиксов). Метод: майнинг-агент (8 кандидатов,
верификационные прогоны) → Jev (`jev-latest`: is_defect/severity/category). Ничего не фикшено.
Кандидаты: `Temp\candidates-stockbasis-r2.json`, вердикты: `Temp\jev-verdicts-stockbasis-r2.json`.

## Вердикты (по убыванию severity)

| # | Кандидат | p | sev | Кат | Суть |
|---|---|---|---|---|---|
| 1 | SB27_ancient_fully_unpriced_sells_leave_silent_pnl_hole | 0.84 | 2.97 | logic | древняя ПОЛНОСТЬЮ unpriced продажа (out, valueUsd 0) съедает лоты FIFO без proceeds и без единого счётчика раскрытия — residual-дыра SB13 для этого подкласса |
| 2 | SB20_sanity_gate_recent_reprice_keeps_stale_partialcash | 0.91 | 2.66 | logic | recent-reprice ветка applySanityGate заменяет valueUsd полным рынком, но не стирает partialCash (делиты только в old-ветке) — счётчик и нота «P&L understated» врут для полностью оценённой сделки (подтверждено прогоном) |
| 3 | SB26_featured_traders_still_requests_tx_version_0 | 0.92 | 2.56 | robustness | скрипт до сих пор шлёт maxSupportedTransactionVersion:0 → v1-тx молча пропускаются через catch — трейдеры с v1-активностью исчезают из ранжирования без skip-сообщения (помечен спорным, Jev уверенно: дефект) |
| 4 | SB21_verifier_empty_answer_still_read_as_true_zero_phantom | 0.87 | 2.50 | logic | пустой value:[] проходит новый гард → сравнение с нулём → ложный «tool holds phantom» с одного зеркала; reconcile.mjs в том же репо требует кросс-подтверждение именно empty-ответа |
| 5 | SB24_featured_shape_fix_desyncs_strip_and_precompute | 0.87 | 2.14 | robustness | bad-shape ветка навсегда снимает retry, а /api/featured перечитывает файл per-request → после излечения файла без рестарта полоса показывает кошельки, которые precompute никогда не посчитает |
| 6 | SB22_unpriced_reason_and_single_source_have_no_consumer (спорный) | 0.58 | 2.11 | robustness | unpricedReason и singleSource не имеют НИ ОДНОГО консьюмера (web/CLI/CSV молча дропают) — раскрытие живёт только в raw JSON |
| 7 | SB23_partialcash_ui_note_misattributes_ancient_cause | 0.80 | 2.05 | robustness | текст ноты («SOL price was unavailable at scan time») противоречит вечной ancient-причине, которую счётчик теперь тоже считает |
| 8 | SB25_isancientday_timestamp_check_mismatched (спорный) | 0.34 | 1.03 | logic | Jev: НЕ дефект — полоса ~16ч doomed-запросов, эффект только perf |

## Выводы

- Фиксы round70 держатся в ядре: гонок в новом advance()/бюджетах rpc НЕ найдено, round68-гонка
  не вернулась, tagless-TTL и isAncientDay чисты. Всё найденное — на периметре фиксов.
- Главный кластер: раскрытие unpriced/ancient неполно (SB27 — тихая дыра P&L, SB22 — флаги без
  консьюмера, SB23 — неверная формулировка ноты, SB20 — врущий счётчик).
- Второй кластер: верификатор/featured (SB21 — фантом выжил через пустой массив; SB24 —
  рассинхрон двух потребителей файла; SB26 — v1-тx).
- Калибровка: SB25 (0.34) — честное «не дефект»; SB26, помеченный спорным, Jev уверенно
  признал дефектом (0.92) — майнер был прав, сомнение снято.

## Судьба находок (вечер 20.09, round71)

Исправлено 7 из 8: SB20–SB24, SB26, SB27 (+тот же v0-паттерн в find-demo-wallet.mjs на
интеграции); SB25 (0.34) — не дефект, не чинилось. Подробнее: REVIEW-2026-09-20.md, round71.
Сьют 206 → 220, зелёный.

