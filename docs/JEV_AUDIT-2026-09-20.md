# Jev-аудит, круг 2 — 20.09.2026

Метод: адверсариальный майнинг кандидатов (суб-агент; исключены SB1–SB8 утреннего круга и
находки REVIEW-2026-09-19) → по каждому кандидату один запрос к TypeSafe System One
(`jev-latest`): `is_defect` (noul), `severity` (score 0–4), `category` (choice).
Ничего не фикшено — только вердикты. Кандидаты: `Temp\candidates-stockbasis.json`,
вердикты: `Temp\jev-verdicts-stockbasis.json`.

## Вердикты (по убыванию severity)

| # | Кандидат | p(дефект) | sev | Категория | Суть |
|---|---|---|---|---|---|
| 1 | SB10_rpc_rotation_burns_429_budget | 0.88 | 3.64 | logic | `-32020`-ротации в rpc.mjs сжигают общий счётчик `attempt`, сокращая 429-бюджет вопреки комментарию фикса; при ≥3 зеркалах + eviction — детерминированный крах скана |
| 2 | SB11_featured_traders_nonarray_json_kills_run | 0.89 | 3.21 | robustness | дырка в фиксе round69: `.sort()` вне try; не-массивный JSON (error-конверт прокси) → TypeError → падение всего прогона; тест покрывает только fetch-rejection |
| 3 | SB17_reconcile_single_endpoint_empty_trust (спорный) | 0.77 | 3.03 | robustness | single-endpoint пустой ответ обнуляет позицию («trust the answer», только WARNING при старте) |
| 4 | SB14_classify_tag_lag_cached_forever | 0.88 | 3.00 | logic | положительный ответ Jupiter с пустыми тегами (лаг индекса) кэшируется навсегда как isStock=false → сделки тикера невидимы (не trade, не transfer, не reconcile) до рестарта |
| 5 | SB13_coingecko_365day_silent_unprice | 0.91 | 2.98 | robustness | подтверждено живым запросом: CoinGecko public API отдаёт 401 для дат старше 365 дней → WSOL-ноги старых сделок молча unpriced, ни один счётчик раскрытия это не считает |
| 6 | SB16_sell_sol_tail_uncapped (спорный) | 0.55 | 2.89 | logic | sell-хвост SOL не ограничен кэпом (асимметрия против пина round68) → посторонний SOL-приток в tx продажи садится в proceeds |
| 7 | SB19_featured_load_nonarray_infinite_retry | 0.86 | 1.77 | robustness | `for...of` без Array.isArray: объект-featured.json → вечный retry, precompute не вооружается; гарды двух потребителей одного файла расходятся |
| 8 | SB12_verify_positions_failed_read_false_phantom | 0.85 | 1.76 | robustness | верификатор при ошибке RPC читает баланс как 0 → ложный вердикт «tool holds phantom»; ровно то, от чего reconcile.mjs сознательно отказался |
| 9 | SB15_ingest_ghost_zero_delta_transfer | 0.87 | 1.42 | logic | `if (tailUnpriced) transfers.push({delta: solTail/1e9})` всегда пушит delta=0, дублируя запись строки 526; не покрыто тестами |
| 10 | SB18_csv_proceeds_unguarded_toFixed (спорный) | 0.17 | 0.71 | robustness | Jev: НЕ дефект — в текущем пайплайне proceedsUsd всегда конечное число (калибровка судьи сработала) |

## Калибровка

Из 3 намеренно спорных кандидатов чистое «не дефект» — только SB18 (0.17, честно помечен
«в проде недостижимо»). SB16 (0.55) и SB17 (0.77) Jev склонился к «дефект» — оба пограничные:
асимметрия гарда реальна, вопрос в достижимости/материальности.
Все 7 «реальных» подозрений получили p ≥ 0.85.

## Судьба находок (вечер 20.09, round70)

Исправлено 8 из 10 (SB10–SB15, SB17, SB19); SB16 запинен комментарием (поведение round8
не тронуто); SB18 — не баг, оставлено. Подробнее: REVIEW-2026-09-20.md. Сьют 188 → 206, зелёный.

